import 'package:flutter/material.dart';

/// Startup screen that shows recent opened request sets and
/// provides access to settings screen and ability to create new request set
/// or manage them
class StartupScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
